- model_class = Game
- title = "#{model_class.model_name.human.titleize} Edit"
- provide(:title, title)

= render :partial => "form"
-# %div.form-actions
  = link_to t('.destroy', :default => t("helpers.links.destroy")), game_path(@game), :method => "delete", :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) }, class: 'btn btn-warning'
